name: sift
description: A lightweight map reading library that can be used in Dart and Flutter projects
version: 1.0.10
homepage: https://github.com/ShoutOutSoftware/Sift-Dart

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  pedantic: ^1.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter